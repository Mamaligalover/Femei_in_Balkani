apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: balkani-frontend
  app.kubernetes.io/component: frontend
  app.kubernetes.io/part-of: balkani

images:
  - name: codbun/balkan
    newTag: latest
